# OCR stack (recommended to install in a separate venv, e.g. .venv_ocr)
# Windows CPU stability notes:
# - paddlepaddle 3.3.x can hit PIR/oneDNN runtime errors; 3.2.2 is more stable.
# - keep numpy <2.4 for paddlex compatibility.

paddlepaddle==3.2.2
paddleocr==3.5.0

# Compatibility pins
numpy==2.3.5
protobuf==6.33.6
PyYAML==6.0.2