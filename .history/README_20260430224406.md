# DATN - Banking Data Pipeline

Dự án xây dựng pipeline dữ liệu ngân hàng theo kiến trúc **Bronze → Silver → Gold**, sử dụng dữ liệu nguồn từ SQL Server. Source database ban đầu gồm các bảng chính như `users`, `cards`, `transactions`, `mcc_codes`.

Mục tiêu của dự án là xây dựng một hệ thống dữ liệu hoàn chỉnh từ tầng dữ liệu gốc đến tầng phục vụ phân tích, bao gồm:

- Kết nối tới database nguồn được cấp quyền truy cập.
- Extract dữ liệu từ source database.
- Nạp dữ liệu vào tầng Bronze.
- Xử lý incremental theo logic PDY / TDY / MNS.
- Xây dựng tầng Silver theo mô hình Data Vault.
- Xây dựng tầng Gold theo mô hình Dimensional Modeling.
- Sử dụng dbt để transform dữ liệu.
- Sử dụng Airflow để điều phối pipeline.
- Phục vụ dashboard Power BI hoặc các phân tích dữ liệu sau này.

---

## 1. Kiến trúc tổng quan của dự án

Pipeline được thiết kế theo kiến trúc nhiều tầng:

```text
Source Database
      |
      |  Python / Airflow Extract
      v
Bronze Layer
      |
      |  dbt Transform
      v
Silver Layer - Data Vault
      |
      |  dbt Transform
      v
Gold Layer - Dimensional Model
      |
      v
Power BI / Analytics / Reporting