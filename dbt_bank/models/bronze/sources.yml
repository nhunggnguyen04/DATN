version: 2

sources:
  - name: bronze
    database: DATN
    schema: bronze
    tables:
      # ── Snapshot hiện tại (dùng cho ref tables) ────────────────────────────
      - name: mcc_codes_tdy

      # ── Current snapshot tables (full data, for satellite/link joins) ────────
      - name: users_tdy
      - name: cards_tdy

      # ── MNS change-set (id + operation_flag: I/U/D) ────────────────────────
      - name: users_mns
      - name: cards_mns
      - name: transactions_mns
      - name: mcc_codes_mns

      # ── Current snapshot tables used to enrich MNS (join on id) ───────────
      - name: transactions_tdy
