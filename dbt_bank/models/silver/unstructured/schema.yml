version: 2

models:
  - name: silver_id_card_documents
    description: "Silver model: normalized CCCD/ID card documents with keys and simple quality flags."
    columns:
      - name: hk_document_bk
        tests:
          - not_null
      - name: document_id
        tests:
          - not_null
      - name: entity_id
        tests:
          - not_null
      - name: run_date
        tests:
          - not_null

  - name: silver_savings_book_documents
    description: "Silver model: normalized savings book documents with keys and simple quality flags."
    columns:
      - name: hk_document_bk
        tests:
          - not_null
      - name: document_id
        tests:
          - not_null
      - name: entity_id
        tests:
          - not_null
      - name: run_date
        tests:
          - not_null
